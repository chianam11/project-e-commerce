const path = require("path");

module.exports = {
    config: path.resolve("src", "config", "config.cjs"),
    "models-path": path.resolve("src", "api", "v1", "models"),
    "migrations-path": path.resolve("src", "api", "v1", "database", "migrations"),
    "seeders-path": path.resolve("src", "api", "v1", "database", "seeders"),
};
